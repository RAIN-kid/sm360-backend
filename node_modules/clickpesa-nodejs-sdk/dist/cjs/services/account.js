"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.AccountService = void 0;
const BASE = '/third-parties/account';
/** Account information methods — balance and statement. */
class AccountService {
    client;
    constructor(client) {
        this.client = client;
    }
    /** Retrieve account balances for all active currencies. */
    async getBalance() {
        return this.client.request('GET', `${BASE}/balance`);
    }
    /**
     * Fetch a transaction statement for a given currency.
     *
     * @param currency `"TZS"` (default) or `"USD"` — required by the API.
     * @param filters.startDate Optional `YYYY-MM-DD` or `DD-MM-YYYY`.
     * @param filters.endDate   Optional `YYYY-MM-DD` or `DD-MM-YYYY`.
     */
    async getStatement(options = {}) {
        return this.client.request('GET', `${BASE}/statement`, undefined, {
            currency: options.currency ?? 'TZS',
            startDate: options.startDate,
            endDate: options.endDate,
        });
    }
}
exports.AccountService = AccountService;
//# sourceMappingURL=account.js.map